[project]
name = "opensens-agent-swarm"
version = "0.1.0"
description = "Opensens Agent Swarm — unified agentic research platform"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["core", "cluster"]

[tool.ruff]
target-version = "py311"
line-length = 100
